# order-sync — บัญชีจอง + ตัดสต็อกมาร์เก็ตเพลส + แจ้งเตือนยกเลิก

ตอบเป็นภาษาไทย กระชับ ตรงประเด็น

## โปรเจคนี้คืออะไร

ดึงออเดอร์เรียลไทม์จากมาร์เก็ตเพลสทุกร้าน เพื่อ 2 เรื่อง

1. **กันขายเกิน** — ปรับจำนวนที่โชว์บนแพลตฟอร์มให้ตรงความจริงระหว่างรอบตัด
2. **จับออเดอร์ยกเลิกก่อนของออกจากร้าน** — ลูกค้ายกเลิกตอนแพ็คเสร็จรอขนส่งมารับ

## 🔑 หลักการสำคัญที่สุด — อ่านก่อนเขียนโค้ดบรรทัดแรก

**นี่ไม่ใช่ระบบสต็อกคู่ขนาน มันคือ "บัญชีจอง"**

Seniorsoft ยังเป็นเจ้าของความจริงของสต็อกเหมือนเดิม ระบบนี้แค่จำว่า
"มีออเดอร์เข้ามากี่ชิ้นตั้งแต่รอบตัดล่าสุด"

```
จำนวนที่ push ไปโชว์บนแพลตฟอร์ม
    = สต็อก Seniorsoft (จากไฟล์ ST รอบล่าสุด)
    − ออเดอร์ที่เข้ามาหลังรอบนั้น (ตารางจอง)
    − บัฟเฟอร์
```

**ทำไมถึงไม่เพี้ยนสะสม:** บัญชีจอง**ล้างตัวเองทุกครั้งที่ตัดรอบ** (วันละ 3 ครั้ง)
ตัวเลขจึงห่างจากความจริงได้มากสุด 4 ชั่วโมง สะสมข้ามวันไม่ได้

**ห้ามทำ:** สร้างตารางสต็อกของตัวเองแล้วบวกลบเอง — จะเพี้ยนจาก Seniorsoft ภายในสัปดาห์เดียว

## บริบทการทำงานจริงของร้าน (อย่าไปเปลี่ยน)

- Seniorsoft Promax = ต้นทางสต็อก **เขียนกลับจากข้างนอกไม่ได้ อ่านอย่างเดียว**
- รอบตัด **08:00 / 12:00 / 14:00** — ดึงออเดอร์ → จัดของ → ยิงบาร์โค้ดเข้า Seniorsoft เพื่อตัดสต็อก
- รอบตัดนี้เป็นกระบวนการของคน **ระบบต้องปรับตาม ไม่ใช่บังคับให้คนปรับตามระบบ**

## ร้านที่ต้องเชื่อม (5 หน้าร้าน)

| แพลตฟอร์ม | ร้าน |
|---|---|
| TikTok Shop | SOLID |
| Shopee | SOLID, REAL, MVP |
| Lazada | SOLID |

**API developer account มีครบทุกแพลตฟอร์มแล้ว**

## งานที่ยากที่สุด: จับคู่ SKU

SKU บนแพลตฟอร์ม ↔ SKU ใน Seniorsoft ถ้าลงไว้ไม่ตรงกันจะจับไม่ได้

- ตรรกะแยกรหัสของ Seniorsoft อยู่ใน `../samchai2004.com/lib/syncParse.js`
- **`../allsitepd/index.html` ทำเรื่องนี้ไว้ครึ่งหนึ่งแล้ว** — เช็คว่า SKU ไหนลงขายช่องไหน (Y/N) → ดูก่อนเขียนใหม่

## แผนเป็นเฟส — อย่าทำรวดเดียว

**เฟส 1 — แจ้งเตือนยกเลิก** *(ส่งของได้เร็วสุด ไม่ต้องรอบัญชีจอง)*
- เช็คออเดอร์ทุก 2-5 นาที
- เจอยกเลิก + สถานะเราคือ "แพ็คแล้ว/รอขนส่ง" → แจ้ง LINE ทันที
- ใช้ `../samchai2004.com/lib/lineNotify.js` ที่มีอยู่แล้ว

**เฟส 2 — บัญชีจอง ร้านเดียวก่อน** (แนะนำ Shopee SOLID)
- ตาราง: `sku, qty, ร้าน, order_id, เวลา, สถานะ`
- คำนวณ → push จำนวนกลับไปแพลตฟอร์ม
- ปุ่ม "ตัดรอบ" → ล้างบัญชีจอง (กดหลังยิงเข้า Seniorsoft เสร็จ)
- **พิสูจน์ว่าเวิร์คกับร้านเดียวก่อน แล้วค่อยขยาย**

**เฟส 3 — ขยายอีก 4 ร้าน**

## มาตรการชั่วคราวที่ใช้ระหว่างยังไม่เสร็จ

กันสต็อกชิ้นสุดท้าย: เหลือ ≤2 ไม่ลงมาร์เก็ตเพลส / เหลือ 1 ขายเฉพาะหน้าร้านกับเว็บเรา
**ถึงระบบเสร็จแล้วก็ยังต้องมีบัฟเฟอร์** ไม่มีระบบไหนซิงก์ทัน 3 แพลตฟอร์มพร้อมกัน 100%

## ❓ คำถามที่ยังไม่ได้ตอบ

1. SKU บนแพลตฟอร์มตรงกับ Seniorsoft ไหม? ถ้าไม่ตรง ต้องทำตารางจับคู่ก่อน
2. ปุ่ม "ตัดรอบ" ให้คนกด หรือหาสัญญาณอัตโนมัติว่ายิงเข้า Seniorsoft เสร็จแล้ว?

## สถานะ

ยังไม่เริ่ม — เริ่มที่เฟส 1 (แจ้งเตือนยกเลิก) ได้เลย ไม่ติดอะไร
