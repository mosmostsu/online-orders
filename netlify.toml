[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# ดึงออเดอร์อัตโนมัติทุก 5 นาที (ตัว scheduled function ยิงเข้า /api/sync/tiktok ให้)
[functions."sync-orders"]
  schedule = "*/5 * * * *"
